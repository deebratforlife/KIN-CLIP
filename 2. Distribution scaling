# Running R (GPU)

# Let us import file and give it a name.

library(ggboxplot)

df <- testing

p <- ggboxplot (df, x = "supp", y = "len",
     color = "black", palette = c("blue", "red", "white")
	 outlier.shape = NA)
	 
p + theme_classic() + ylim(0, 0.075) + geom_point (aes(color = supp),
    position = position_jitterdodge(jitter.width = 6.0), cex = 0.3, alpha = 0.3)
	
# supp is the x axis metric and len is the y axis metric.
# metrics are defined based on the identity of data matrix.
# 3 colors are used. Colors can be added based on the requirement.

# For obtaining p-value of inter-group differences:

library(ggpubr)

p + stat_compare_means(method = "ANOVA")

